{
  "name": "minimo-studio-extension",
  "version": "1.0.3",
  "description": "Minimo Studio — local image converter and studio for Chrome",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "icons": "node scripts/generate-icons.js",
    "build": "tsc && vite build",
    "pack": "pnpm run build && node scripts/pack-extension.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "@types/node": "^22.13.4",
    "sharp": "^0.35.4",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
